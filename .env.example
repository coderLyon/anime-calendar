# Supabase 匿名云同步（可选）：不配置则站点以纯本地 localStorage 模式运行。
# 配置方式：Supabase 项目 → Authentication → Providers 开启匿名登录 →
# 复制 Project URL 与 anon public key；本地复制为 .env.local，CI 在仓库 Variables 注入同名变量。
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
